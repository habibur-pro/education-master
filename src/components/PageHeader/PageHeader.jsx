

const PageHeader = ({ title }) => {
    return (
        <div className="py-40 bg-primary text-white">
            <h1 className="text-5xl text-center font-bold">{title}</h1>
        </div>
    );
};

export default PageHeader;